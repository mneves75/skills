# 2026-08-26 — 1.6.0: mneves-eli5

- Added `skills/mneves-eli5` (Feynman technique, 5 audience levels, answers in user language).
- Installed for all agents via `~/.agents/skills/mneves-eli5` → repo, then per-farm links (rule: every farm with ≥5 links into the shared catalog).
- Noticed: other `mneves-*` skills are only in `~/.claude/skills`, not the shared catalog.

# 2026-08-26 — 1.8.0: public-repo hardening + `npx skills`

- Scanned tree + full history for PII/secrets: clean (authors already GitHub noreply). No history
  rewrite; squashing would only force-push a public `main` with tags for zero benefit.
- Fixed: `~/dev/GUIDELINES-REF` in `mneves-expert-review` (was shipped in the description),
  `.codemap/` ignored only by the global gitignore, no CI, no SECURITY.md/Dependabot, unpinned
  actions, stale AGENTS/CLAUDE trees, obsolete `docs/EXAMPLES-EXEC-SPEC.md`.
- `npx skills@latest add mneves75/skills --list` already discovers all six skills — support is
  documentation + the CI layout check (folder == `name`, no root `SKILL.md`).
