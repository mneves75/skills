# 2026-08-18 — 1.5.0: mneves-verify + repo ast-grep guard

## Shipped

- **`mneves-verify`** (`skills/mneves-verify/`) — independent-verification skill. Encodes the
  "never grade your own homework" rule: fresh independent context, evidence routes by artifact
  type, one correction + one reverify then PASS/FAIL/BLOCKED, PASS alone permits done/fixed/shipped.
  Deliberately machinery-free (no scripts/deps/references/scaffolding).
- **ast-grep guard**: `sgconfig.yml` + `rules/no-as-any.yml` (TypeScript) + `rules/no-as-any-tsx.yml`
  (TSX) — two rules rejecting `as any`. Current tree passes; `unknown` + narrowing guard is the
  replacement.
- **Blocking pre-commit hook** (`.githooks/pre-commit`): runs the ast-grep scan, fails closed
  when ast-grep is absent. `git config core.hooksPath .githooks` set locally.
- **Memory files** created (previously absent): `FOR_YOU_KNOW.md`, `MEMORY.md`, this entry.

## Validation

- ast-grep scan clean (exit 0); planted `as any` returns exit 1 with the rule message.
- Hook `bash -n` clean, executable, fail-closed on missing ast-grep.
- Frontmatter `name: mneves-verify` matches directory; internal links resolve.
- `git diff --check` clean.

## Open items

- None.
