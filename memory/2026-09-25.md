# 2026-09-25 — 1.24.0: review moves to Astra

- User request: review work on `gpt-6-astra` `high`, falling back to `gpt-6-sol` `xhigh`.
- autoreview: tests changed first (12 red), then the two constants and `--help` text; full suite
  green (55 + 320, 1 skipped). Explicit `--model gpt-6-astra` now keeps the retry, since it equals
  the default; `--thinking none|minimal` without a model now fails (Astra rejects them).
- Probes: `codex exec` answered OK for `gpt-6-astra`/`high` and `gpt-6-sol`/`xhigh`; a fake id exited 1.
- Host aligned (outside the repo): `codex-auto` review default, the five reviewer-role agent TOMLs,
  `agent-runtime.md`, and the `~/.agents/skills/autoreview` copy. `codex-auto review` has no
  automatic retry; the Sol fallback there is the `CODEX_AUTO_REVIEW_MODEL`/`_EFFORT` override.

## 1.24.1: review effort rises to xhigh

- User request: all review skills on `gpt-6-astra` `xhigh`. In this repo only `autoreview` and the
  orchestrator Defaults pin review models; `mneves-verify`, `mneves-expert-review` and
  `mneves-superaudit` route model review through `autoreview`, so they follow it.
- Tests changed first (2 autoreview tests red: `'high' != 'xhigh'`), then `DEFAULT_THINKING_BY_ENGINE`
  and `--help`. The plan advisor seat moved too, as a review role.
- Host: `~/.zshrc` pinned `AUTOREVIEW_CODEX_THINKING=high`, which would have overridden the new
  default; now `xhigh`. `~/bin/codex-auto-test` was already red before this change (exec assertions
  expected Astra; exec is Sol) and was corrected alongside the new review-effort assertion.
- Codex 0.157 has `review_model` but no review-effort key, so built-in `/review` uses the session effort.
