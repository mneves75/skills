# 2026-09-23 — 1.23.3: skills that were never installed

- Symptom: a Claude session said `mneves-verify` did not appear in its skill list.
- Cause: on this Mac every agent directory links each skill separately. `mneves-verify`,
  `mneves-handoff` and `mneves-agent-readiness` had never been linked into `~/.agents/skills`
  or any agent directory; the other seven were.
- Host fix (outside the repo): `~/.agents/skills/<name>` links to `skills/<name>` here, and each
  of the 13 agent skill directories (Claude, Codex, OpenCode, Cursor, Gemini, Factory, jcode, pi,
  Grok, Devin, Copilot, Hermes, Command Code) links to `~/.agents/skills/<name>`. Each link was
  checked to reach a `SKILL.md` whose `name:` matches. The existing `handoff` links are
  mattpocock's skill and were left alone.
- Shipped: README "Check the install" loop (tested against a real directory and an empty one)
  and a HOWTO pointer. Lesson: adding a skill here means linking it on every agent too.
