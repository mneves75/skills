# 2026-09-22 — 1.19.0: review against 2026 guidance

- Sources checked: Anthropic Agent Skills overview + best practices, Claude Code skills and
  sub-agents references, agentskills.io spec and `skills-ref` 0.1.5 source, Codex docs
  (build-skills, config reference, non-interactive mode, subagents), Anthropic and OpenAI model
  lists, Codex CLI 0.155.0-alpha.9.2 `--help`, upstream `steipete/agent-scripts codex-first`.
- Found: "Opus 5.1" (Defaults, HOWTO) is not a model; current Claude ids are `claude-fable-5-1`
  and `claude-opus-5`. Codex loads skills from `.agents/skills` / `~/.agents/skills`, not
  `~/.codex/skills`. `skills-ref validate` errors on any non-spec frontmatter key (proved with a
  scratch `context: fork` skill). `codex exec` gained `fork`; global flags must precede `resume`
  (codex-lane already complies). Resume can be refused on long threads (`thread/resume failed`)
  and codex then echoes the previous final message with a non-zero exit.
- Decisions: Claude seats named by alias ("latest release"); Codex seats keep 1.18.1 routing (Sol
  xhigh executes, Astra high orchestrates/advises/reviews) — the uncommitted "Sol everywhere"
  diff was stashed, only its autoreview authorization paragraph was kept. Frontmatter stays
  spec-only; `compatibility` added to the three executable skills.
- Shipped: `codex-lane next` captures codex stderr beside the events, relays it, and on a refused
  resume exits non-zero, says so, keeps the previous `last` pointer, and withholds the stale
  message. `tools/codex-lane.test` (fake `codex` on PATH, positive + negative control) is a CI
  step; the pre-change script fails it.
- Docs: codex-dispatch reference restructured (flags/resume/fork, failure classes, optional
  launcher marked not shipped); README "Lane mechanics"; HOWTO persona fixed ("main session",
  not "advisor"); `/path/to/myapp` replaces the author-layout example; live autoreview harness
  documented; sm2 reference linked.
- Outside this repo (reported, not changed): `~/.claude/reference/agent-runtime.md` still says
  "Opus 5.1" and routes review to Sol; `~/bin/codex-auto` and `~/.codex/agents/*advisor*.toml`
  default review to Sol, which now disagrees with the skill's Astra reviewer seat.

## 1.20.0 (same day)

- Acted on the 1.19.0 self-assessment: `codex-lane fork` (branch a lane, inherit cwd and policy,
  refuse a fork that reports the source id) with test coverage; `tools/scripts/check-defaults-block.sh`
  in CI keeps ids inside the Defaults block and requires the verified-on date (planted `gpt-9-zeta`
  outside the block fails it). Probe confirmed `gpt-6-astra` at `high` answers on this account.
- Host aligned to the skill: `~/bin/codex-auto` review defaults, six review/advice agent TOMLs, and
  `~/.claude/reference/agent-runtime.md` now say Astra high for advice and review, Sol xhigh for
  execution, Claude seats by alias.
- Remote tags stop at v1.9.0; pushed v1.19.0 (1374c30) and v1.20.0. 1.10–1.18.1 remain untagged.

## 1.21.0 (same day)

- Claude main seat Fable -> Opus. Probe: `claude -p --model claude-opus-5-5` and `--model opus`
  both answered on `claude-opus-5-5` (CLI 2.1.280). autoreview `claude` default -> `claude-opus-5-5`;
  explicit `fable` keeps its 2.1.170 floor. All gates green (243 autoreview tests); a planted
  `opus` outside the Defaults block fails the guard.
- `~/.agents/skills/autoreview` had drifted to the superseded Sol-everywhere variant (codex Sol
  xhigh, no fallback). It is a copy, not a link; re-synced from this repo with rsync, so it now
  matches (Astra high + Sol access-only fallback). Re-sync it after every autoreview change.
